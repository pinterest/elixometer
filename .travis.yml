language: elixir
elixir: 1.0.0
otp_release: 17.4
matrix:
  include:
    - elixir: 1.4.0
      otp_release: 19.2
sudo: false
after_script:
  - MIX_ENV=test mix coveralls.travis
cache:
  directories:
  - $HOME/.mix/archives
