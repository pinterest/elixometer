language: elixir
elixir: 1.2.6
otp_release: 18.2
matrix:
  include:
    - elixir: 1.4.0
      otp_release: 19.2
install:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get
after_script:
  - MIX_ENV=test mix coveralls.travis
cache:
  directories:
  - $HOME/.mix/archives
